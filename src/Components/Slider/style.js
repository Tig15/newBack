import {StyleSheet} from 'react-native';

export const sliderStyles = StyleSheet.create({
  swiper: {
    marginTop: 12,
  },
  imageSwipe: {
    width: '100%',
    height: 140,
    objectFit: 'fill',
    borderRadius: 5,
  },
});
